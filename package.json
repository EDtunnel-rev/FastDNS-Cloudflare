{
  "name": "OpenDNS-Cloudflare",
  "version": "1.0.0",
  "description": "A complex DNS server running on Cloudflare Workers, supporting A, AAAA, CNAME, DNAME, MX, and TXT records.",
  "main": "worker_obfuscates.js",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "build": "echo 'No build step needed for this project'"
  },
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "dotenv": "^16.0.3"  // Assuming you might use dotenv for environment variables
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
